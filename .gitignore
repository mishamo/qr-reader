# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work

# Dependency directories
vendor/

# Build output
qr-scanner-app
QR Scanner.apk
*.apk

# IDE specific files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment files (contains OAuth secrets)
.env
.env.local
.env.backup

# OAuth tokens (stored locally on device)
oauth_token.json
token.json
auth_token
duplicate_mode
active_sheet
user_email

# Generated credentials file (contains secrets)
internal/auth/credentials.go

# Fyne bundle
bundled.go

# Test files
test_*.go

# Icon creation script output (keep Icon.png)
create_icon.go

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/

# Flutter build artifacts
build/
/build/
android/.gradle/
android/gradle/
android/local.properties
*.iml
*.ipr
*.iws